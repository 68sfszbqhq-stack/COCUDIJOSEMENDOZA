rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /pln_topics/{topic} {
      allow read: if true;
      allow create, update: if request.resource.data.passcode == "ClaveDocente2026";
      allow delete: if false;
    }
  }
}
